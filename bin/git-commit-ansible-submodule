#!/usr/bin/env zsh
# Update the ./config/ansible/galactica submodule with message:
# chore(pdf): Update ansible submodule

cd $(git root)
git add ./config/ansible/galactica
git commit -m "chore(ansible): Update ansible submodule"
